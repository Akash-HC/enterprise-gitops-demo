apiVersion: argoproj.io/v1alpha1
kind: AppProject
metadata:
  name: thinkpad-project
  namespace: argocd
spec:
  description: Enterprise argoCD deployment environment

  sourceRepos:
    - '*'

  destinations:
    - namespace: thinkpad-enterprise
      server: https://kubernetes.default.svc

  clusterResourceWhitelist:
    - group: '*'
      kind: '*'
